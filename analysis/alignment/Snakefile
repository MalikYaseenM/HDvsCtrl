
rule fastqc:
    input:
        "../../samples/{sample}.fastq.gz"
    output:
        "../../samples/{sample}_fastqc.zip"
        "../../samples/{sample{sample}_fastqc.html"
    shell:
        "fastqc {input} - > {output}"

